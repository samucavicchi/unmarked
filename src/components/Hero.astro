---
// Hero. Riusabile in homepage e potenzialmente in altre landing.
// Accetta props per personalizzare. Default = home.

export interface Props {
  tag?: string;
  title?: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
  meta?: string;
}

const {
  tag = 'Reportage · Fotografia · Itinerari',
  title = 'I luoghi che non trovi<br>sulle guide',
  subtitle = 'Unmarked racconta i posti che esistono ancora. Reportage fotografici, itinerari scritti da chi ci è stato davvero, storie che non seguono le stagioni turistiche.',
  ctaLabel = 'Esplora le destinazioni',
  ctaHref = '/destinazioni',
  meta = 'Reportage · Itinerari · Consulenze di viaggio'
} = Astro.props;
---

<div class="hero">
  <div class="hero-bg"></div>
  <div class="hero-overlay"></div>
  <div class="hero-tag">{tag}</div>
  <h1 class="hero-title" set:html={title}></h1>
  <p class="hero-sub">{subtitle}</p>
  <div class="hero-bottom">
    <a href={ctaHref} class="btn-hero">{ctaLabel}</a>
    <span class="hero-meta">{meta}</span>
  </div>
</div>
